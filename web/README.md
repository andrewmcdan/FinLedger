# public

Static frontend assets and the single-page app shell.

- `index.html` holds the shared layout shell.
- `pages/` stores HTML fragments loaded by the router.
- `css/` contains base, layout, and component styles.
- `js/` holds the router, page controllers, and helpers.
- `images/` stores image assets.
