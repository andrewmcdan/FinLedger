# public/js/pages

Per-page controllers loaded by the client-side router.

Files in the root of this directory are subject to authorization checks.

Files in the `public` subdirectory are accessible without authentication.