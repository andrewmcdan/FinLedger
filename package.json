{
  "name": "finledger",
  "version": "0.1.0",
  "private": true,
  "description": "Web-based accounting and financial management system",
  "scripts": {
    "start": "node src/server.js --env-file=.env",
    "dev": "nodemon src/server.js --env-file=.env",
    "test": "node --test"
  },
  "dependencies": {
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
